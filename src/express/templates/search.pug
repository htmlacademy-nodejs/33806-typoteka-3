extends ./layout/base
block vars
  - var classes = ['wrapper-color']
block content
  main
    section.search-page
      div(class=!value ? "search-page__wrapper" : "search-page__wrapper search-page__wrapper--empty")
        h1 Поиск
        .search.search-page__form
          form(action='/search', method='post')
            label
              input(type='text', name='value', placeholder='Что ищем?', value=value)
            button.search__button.button.button--transparent(type='submit') Найти
          if articles
            each article in articles
              .search__result
                time.search__date(datetime='2019-03-21T20:33') 21.03.2019, 20:33
                a(href='/articles/' + article.id).search__link= article.title
          if value
            p.search-page__empty= 'Ничего не нашлось'
