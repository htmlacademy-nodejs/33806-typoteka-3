mixin previews(previews)
  section.main-page__list.preview
    h2.visually-hidden Список превью статей
    if previews
      ul.preview__list
        each preview in previews
          li.preview__item
            if preview.category
              ul.preview__breadcrumbs
                each category in preview.category.slice(0, 5)
                  li.preview__breadcrumbs-item
                    a.preview__breadcrumbs-link(href='/category/' + category) #{category}
            .preview__background
              img.preview__background-image(src='img/skyscraper@1x.jpg', width='460', height='240', srcset='img/skyscraper@1x.jpg 1x, img/skyscraper@2x.jpg 2x', alt='Фотография небоскреба')
            time.preview__time(datetime='2019-03-21T20:33')=preview.createdDate
            h3.preview__name
              a.preview__name-link(href='/article/' + preview.id)=preview.title
            p.preview__text=preview.announce
            a.preview__comment(href='#')
              | Комментарии
              span.preview__cloud
              b.preview__comment-count=preview.comments.length
